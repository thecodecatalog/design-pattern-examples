// Receiver
// The Receiver knows how to perform an action.

module.exports = {
  add: (x, y) => {
    return x + y;
  },
  subtract: (x, y) => {
    return x - y;
  },
  multiply: (x, y) => {
    return x * y;
  },
  divide: (x, y) => {
    return x / y;
  }
};
